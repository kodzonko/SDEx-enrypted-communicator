import { createNavigationReducer } from 'react-navigation-redux-helpers';
import AppNavigatorContainer from 'app/components/AppNavigator';

export default createNavigationReducer(AppNavigatorContainer);
